# Local cluster for ai-flow: `make dev-up`.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: ai-flow
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080   # ai-flow UI (NodePort)
        hostPort: 8080
        listenAddress: 127.0.0.1
